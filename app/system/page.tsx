import {getUserList} from "../lib/service/user"
import List from "./list"

const SystemPage: React.FC = async() => {

    return (
        <main>
            <List/>
        </main>
    )
}

export default SystemPage